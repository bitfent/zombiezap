//! ZombieZap client. M3 brings the Bevy app; this stub proves workspace wiring.

fn main() {
    println!(
        "zz-client (protocol v{}) — no window yet",
        zz_core::PROTOCOL_VERSION
    );
}
